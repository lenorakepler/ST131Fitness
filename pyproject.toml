[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phylo-fit"
version = "0.1.0"
dependencies = [
    "click",
]

[project.scripts]
fit = "cli:cli"

[tool.setuptools.packages.find]
where = ["src"]